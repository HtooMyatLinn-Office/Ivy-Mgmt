# Put this file in the project root. If you only keep vars in src/.env, that is loaded too.
DATABASE_URL="postgresql://postgres:YOUR_DB_PASSWORD@localhost:5432/pos_system?schema=public&connect_timeout=10&sslmode=prefer"
PORT=3000
